{
  "name": "plugin-linkedin-poster",
  "version": "0.1.0",
  "description": "Paperclip plugin for automated LinkedIn posting with content calendar and image support",
  "type": "module",
  "main": "dist/worker.mjs",
  "paperclipPlugin": {
    "manifest": "dist/manifest.mjs"
  },
  "scripts": {
    "build": "node build.mjs",
    "prepare": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@paperclipai/plugin-sdk": ">=2026.416.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "typescript": "^5.7.0"
  }
}
